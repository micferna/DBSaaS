# Cargo audit configuration
# https://rustsec.org/

[advisories]
# RUSTSEC-2023-0071: rsa crate Marvin Attack via sqlx-mysql
# FALSE POSITIVE: sqlx-mysql is an optional dependency of sqlx-macros-core that
# appears in Cargo.lock but is NOT compiled. We only use PostgreSQL (no MySQL features enabled).
# The rsa crate is never included in our binary. Verified with `cargo tree -i sqlx-mysql`.
ignore = ["RUSTSEC-2023-0071"]
